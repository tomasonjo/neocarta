# Neo4j LPG Ingest Module

This module contains code for ingesting Label Property Graph (LPG) metadata into Neo4j to create a semantic layer graph. 

This includes metadata from
* Neo4j APOC schema calls